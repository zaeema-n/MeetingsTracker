# MeetingsTracker — global ministry pack schema
# Same layout for every ministry folder under data/. Drives OpenGIN ingestion mapping.

version: 1

overview: |
  One ministry's dataset under data/<Ministry name>/. President (Person/citizen) scopes
  resolve for shared org; boards/councils and other files are created with explicit ids.
  Meetings hold types and instances. RTIs record requests and what they discovered.

pack:
  requires_active_at: true
  files:
    acts: acts.yaml
    organisations: organisations.yaml
    meetings: meetings.yaml
    rtis: rtis.yaml

entities:
  act:
    file: acts
    collection: acts
    id_field: id
    name_field: name
    kind: { major: Document, minor: Act }
    default_ingest: create

  president:
    file: organisations
    collection: president
    id_field: id
    name_field: name
    kind: { major: Person, minor: citizen }
    default_ingest: resolve

  ministry:
    file: organisations
    collection: president → ministry
    id_field: id
    name_field: name
    kind: { major: Organisation, minor: Ministry }
    default_ingest: resolve
    parent_relationships:
      - relation: AS_MINISTER
        parent_type: president
        parent_id_from: _parent_president_id

  department:
    file: organisations
    collection: president → ministry → department
    id_field: id
    name_field: name
    kind: { major: Organisation, minor: Department }
    default_ingest: resolve
    parent_relationships:
      - relation: AS_DEPARTMENT
        parent_type: ministry
        parent_id_from: _parent_ministry_id

  board:
    file: organisations
    collection: president → ministry → department → board
    default_ingest: create
    id_field: id
    name_field: name
    kind: { major: Organisation, minor: Board }
    parent_relationships:
      - relation: AS_BODY
        parent_type: department
        parent_id_from: _parent_department_id
      - relation: AS_BODY
        parent_type: ministry
        parent_id_from: _parent_department_id

  council:
    file: organisations
    collection: president → ministry → department → council
    default_ingest: create
    id_field: id
    name_field: name
    kind: { major: Organisation, minor: Council }
    parent_relationships:
      - relation: AS_BODY
        parent_type: department
        parent_id_from: _parent_department_id
      - relation: AS_BODY
        parent_type: ministry
        parent_id_from: _parent_department_id

  meeting:
    file: meetings
    collection: meetings
    id_field: id
    name_field: name
    kind: { major: Event, minor: Meeting }
    default_ingest: create

  meeting_instance:
    file: meetings
    collection: meetings → instances
    id_field: id
    name_field: name
    kind: { major: Event, minor: MeetingInstance }
    default_ingest: create
    parent_relationships:
      - relation: HAS_EVENT
        parent_type: meeting
        parent_id_from: _parent_meeting_id

  rti_document:
    file: rtis
    collection: rti_documents
    id_field: id
    name_field: name
    kind: { major: Document, minor: RTI }
    default_ingest: create
    # attributes:
    #   - key: summary
    #     source: summary

links:
  - field: mandated_by
    relation: mandates
    target: act

  - field: meetings
    relation: has_event
    at: [ministry, department, board, council]
    target: meeting
    many: true

  - field: recipient_id
    relation: sent_to
    at: sent_to
    target: [ministry, department, board, council]

  - field: discovered_events
    relation: discovers
    target: meeting_instance
    many: true
